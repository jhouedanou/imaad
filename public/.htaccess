# IMAAD — config Apache pour hébergement cPanel (public_html)
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Sert directement les fichiers et dossiers existants (assets, images, pages prérendues)
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # Sinon, retombe sur l'app (routes Vue Router non prérendues)
  RewriteRule ^ index.html [L]
</IfModule>

# Cache long pour les assets versionnés
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/webp        "access plus 1 year"
  ExpiresByType image/png         "access plus 1 year"
  ExpiresByType image/svg+xml     "access plus 1 year"
  ExpiresByType text/css          "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"
</IfModule>
